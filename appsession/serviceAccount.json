# Navegar al directorio del proyecto
cd c:\Users\punto\Downloads\whatsapp-bot-dialogflow-firestore

# Convertir el archivo de credenciales a base64
$jsonContent = Get-Content -Path ".\serviceAccount.json" -Raw
$base64 = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($jsonContent))

# Configurar el secreto en Fly.io
fly secrets set GOOGLE_APPLICATION_CREDENTIALS_JSON=$base64

# Navegar al directorio del proyecto
cd c:\Users\punto\Downloads\whatsapp-bot-dialogflow-firestore

# Convertir el archivo de credenciales a base64
$jsonContent = Get-Content -Path ".\asistentedependencia-mwqx-06544989c430.json" -Raw
$base64 = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($jsonContent))

# Configurar el secreto en Fly.io
fly secrets set GOOGLE_APPLICATION_CREDENTIALS_JSON=$base64